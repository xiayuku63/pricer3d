services:
  app:
    build: .
    container_name: pricer3d-app
    restart: unless-stopped
    ports:
      - "127.0.0.1:5000:5000"
    volumes:
      - .:/app
      - /app/venv
      - /app/__pycache__
      - pricer3d_data:/app/data
    env_file:
      - .env
    environment:
      - UPLOADS_DIR=/app/data/uploads
      - OUTPUTS_DIR=/app/data/outputs
      - USER_DATA_DIR=/app/data/user
      - DB_PATH=/app/data/app.db

volumes:
  pricer3d_pricer3d_data:
    external: true
